## InstaBatch (Desktop)

### Dev (Linux/macOS/Windows)

- `npm run dev`

### Build (renderer)

- `npm run build`

### Windows installer (.exe)

No Windows:

- `npm run dist:win`

Isso baixa `yt-dlp.exe` e `gallery-dl.exe` para `resources/bin/win/` (não versionado no git) e empacota com `electron-builder` (NSIS).
